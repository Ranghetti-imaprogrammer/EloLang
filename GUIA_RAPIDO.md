📘 Guia de Referência Rápida: Elo v1.1
A Elo foi desenhada para ser lida como um roteiro e executada como uma ferramenta de precisão.

1. Declaração e Entrada
seja [nome] = [valor] : Cria uma variável.

seja [nome] = escrever "[pergunta]" : Interage com o usuário e guarda a resposta.

2. Comunicação e Dados
buscar em "[url]" : Realiza um GET HTTP e converte para objeto automaticamente.

gravar [variável] em "[arquivo].json" : Salva dados de forma estruturada no disco.

3. Lógica de Controle
caso [condição] { ... } : Executa se a comparação (ex: >, <, ==, >=) for real.

repetir [item] em [lista] { ... } : Itera sobre cada elemento de um conjunto de dados.

4. Saída e Tempo
exibir [mensagem] : Mostra texto colorido no terminal.

esperar [n]s : Pausa o fluxo (útil para não sobrecarregar APIs).